为了以后清楚每一次修改的内容，我决定使用一个在文件前7行的开发记录来记录改动。
以下是具有测试能力的子代码：
Button.py 内置一个类，测试代码生成一个当鼠标置于图片上即变色的按钮类。
mapBlock.py 内置一个map块和基于它各种颜色的子类map块，通过测试代码你可以明白地图的生成原理（当然，我还没画地图，只是用了一个24×16 的二维数组来做地图，地图由24×16 个绿色块组成）。
下面是特殊代码介绍：
globalValue.py 该代码内有一个类用于记录多文件共享的真全局变量，文件引用全局变量是只需将该代码导入即可。同时该代码下还有我本来想写成readme的蹩脚英语注释。
mainGame.py 是整个游戏的入口通过在main中导入并使用run函数来运行游戏主体，恩，这是一个大坑
main.py 整个程序的入口，也是主界面，主界面按钮还需要重新布局的，太丑了
Chinese 文件内是我们要使用的字体包，后期修改字体和使用其他字体都放在这里
src 是游戏的资源文件包，内有两个文件夹，以后还会有更多，游戏就是这样，把一堆图片跑起来，就很有意思了。
期待你的成长，和你的发际线。加油！————lua
